Алгоритми та структури даних — Двійкові дерева та ієрархічні структури
📂 Структура проєкту

.
├── task-1/
│   ├── avl_tree.py
│   ├── test_avl_tree.py
│   └── README.md
│

├── task-2/
│   ├── avl_tree.py
│   ├── test_avl_tree.py
│   └── README.md
│

├── task-3/
│   ├── avl_tree.py
│   ├── test_avl_tree.py
│   └── README.md
│

└── task-4/
    ├── comment.py
    ├── test_comment.py
    └── README.md



Кожна задача:

повністю ізольована

має власний README

містить реалізацію + тести

🟦 Task 1 — Пошук максимального значення в AVL-дереві
Мета

Реалізувати алгоритм, який знаходить найбільше значення у:

двійковому дереві пошуку (BST)

або AVL-дереві

Ключова ідея

У будь-якому BST / AVL-дереві:

найбільше значення завжди знаходиться у крайньому правому вузлі.

Алгоритм

Почати з кореня

Рухатись вправо, доки існує правий нащадок

Повернути значення останнього вузла

Складність

Часова: O(log n) для AVL

Просторова: O(1)

Тести

Перевіряється:

стандартне дерево

дерево з одним елементом

порожнє дерево (виняток)

🟦 Task 2 — Пошук мінімального значення в AVL-дереві
Мета

Реалізувати алгоритм знаходження найменшого значення в дереві.

Ключова ідея

У будь-якому BST / AVL-дереві:

найменше значення завжди знаходиться у крайньому лівому вузлі.

Алгоритм

Почати з кореня

Рухатись вліво, доки існує лівий нащадок

Повернути значення

Складність

Часова: O(log n) для AVL

Просторова: O(1)

Тести

Перевіряється:

збалансоване дерево

один елемент

порожнє дерево

🟦 Task 3 — Обчислення суми всіх значень у дереві
Мета

Знайти суму всіх значень у BST / AVL-дереві.

Підхід

Використано рекурсивний DFS-обхід дерева.

Формула:

sum(node) = node.value + sum(left) + sum(right)


Порядок обходу не має значення.

Складність

Часова: O(n) — відвідується кожен вузол

Просторова: O(h) — глибина рекурсії

Тести

Перевіряється:

повне дерево

один елемент

порожнє дерево (результат = 0)

🟦 Task 4 — Ієрархічна система коментарів (додаткове)
Мета

Реалізувати ієрархічну структуру коментарів, де:

кожен коментар може мати відповіді

відповіді також можуть мати відповіді

структура утворює дерево довільної глибини

Основні вимоги

клас Comment

поля:

текст

автор

список відповідей

прапорець is_deleted

методи:

add_reply

remove_reply (логічне видалення)

display (рекурсивний вивід)

Архітектурний патерн

Composite Pattern

логічне видалення без руйнування структури

Особливості реалізації

Видалений коментар:

не зникає з дерева

зберігає відповіді

виводиться стандартним текстом

Відступи — 4 пробіли на рівень

Тести

Покрито:

велике дерево (4+ рівнів)

кілька гілок

видалення:

верхнього рівня

глибокого вузла

додавання відповіді після видалення

дерево з одним коментарем

▶ Як запускати будь-яку задачу

Перейти в потрібну папку та виконати:

python test_avl_tree.py


або

python test_comment.py