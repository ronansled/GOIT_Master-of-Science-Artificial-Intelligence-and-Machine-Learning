# Завдання 2: Порівняння алгоритмів DFS та BFS

## 📋 Опис

Це завдання присвячене реалізації та порівнянню двох фундаментальних алгоритмів обходу графа:
- **DFS (Depth-First Search)** — пошук у глибину
- **BFS (Breadth-First Search)** — пошук у ширину

---

## 🎯 Мета

Розуміти різницю між двома основними стратегіями обходу графа та їхній вплив на результати пошуку шляхів.

---

## 📊 Висновки

### 1. **Різниця в результатах DFS та BFS**

| Характеристика | DFS | BFS |
|---|---|---|
| **Стратегія** | Йде вглиб, досліджує гілки до кінця | Йде вширину, досліджує всіх сусідів |
| **Структура даних** | Стек (рекурсія) | Черга (queue) |
| **Найкоротший шлях** | ❌ Не гарантує | ✅ Гарантує |
| **Використання пам'яті** | Менше (для глибоких графів) | Більше (для широких графів) |
| **Час виконання** | O(V + E) | O(V + E) |

### 2. **Аналіз знайдених шляхів**

Для тестової пари `Python_programming → Data_science`:

- **DFS шлях:** Рекурсивно спускається в першого сусіда, потім його сусідів, і т.д., поки не знайде мету
- **BFS шлях:** Послідовно досліджує всіх сусідів, потім сусідів сусідів, гарантуючи найкоротший шлях

### 3. **Практичне застосування**

#### 🔍 **Коли використовувати DFS:**
- Пошук у дереві рішень
- Топологічне сортування
- Виявлення циклів у графі
- Задачі з обмеженим простором пам'яті

#### 🔍 **Коли використовувати BFS:**
- Пошук найкоротшого шляху (невважена граф)
- Пошук за рівнями (level-order traversal)
- Соціальні мережі (знайти друзів на відстані N)
- Лабіринт (найкоротший вихід)

### 4. **Особливості реалізації**

#### DFS (Рекурсивна реалізація)
```python
def dfs_path(graph, start, goal, path=None):
    if path is None:
        path = []
    path = path + [start]
    
    if start == goal:
        return path
    
    for neighbor in graph.neighbors(start):
        if neighbor not in path:
            new_path = dfs_path(graph, neighbor, goal, path)
            if new_path:
                return new_path
    return None
```

**Переваги:**
- ✅ Простіша реалізація
- ✅ Менше використовує пам'ять (для глибоких графів)

**Недоліки:**
- ❌ Не гарантує найкоротший шлях
- ❌ Небезпека переповнення стеку для дуже глибоких графів

#### BFS (Ітеративна реалізація)
```python
def bfs_path(graph, start, goal):
    queue = deque([[start]])
    visited = set()
    
    while queue:
        path = queue.popleft()
        node = path[-1]
        
        if node == goal:
            return path
        
        if node not in visited:
            visited.add(node)
            for neighbor in graph.neighbors(node):
                new_path = list(path)
                new_path.append(neighbor)
                queue.append(new_path)
    return None
```

**Переваги:**
- ✅ Гарантує найкоротший шлях
- ✅ Безпечніша від переповнення стеку

**Недоліки:**
- ❌ Більше використовує пам'ять (для широких графів)
- ❌ Складніша реалізація

### 5. **Емпіричні спостереження**

На основі тестування на графі знань (15 вершин, 16 ребер):

1. **DFS часто знаходить довші шляхи** — тому що йде глибше перед тим, як розглянути альтернативи
2. **BFS завжди знаходить найкоротший шлях** — систематично досліджує вершини за рівнями
3. **Для розв'язних графів обидва алгоритми знаходять шлях** — але BFS дає оптимальний результат

### 6. **Вибір алгоритму у практиці**

```
Потрібен найкоротший шлях? → BFS
                ↓
Мало пам'яті, глибокий граф? → DFS
                ↓
Потрібна гнучкість? → Розглянути A* або Dijkstra
```

---

## 🧪 Результати тестування

### Приклад 1: `Python_programming → Data_science`
- **DFS:** `Python_programming → NetworkX → Graph_theory → Data_science` (4 кроки)
- **BFS:** `Python_programming → Pandas → Data_science` (3 кроки) ✅ **Коротше**

### Приклад 2: `Wikipedia → Business_intelligence`
- **DFS:** `Wikipedia → Data_science → Power_BI → Business_intelligence` (4 кроки)
- **BFS:** `Wikipedia → Data_science → Power_BI → Business_intelligence` (4 кроки) ✅ **Одинаково**

---

## 🔑 Ключові висновки

1. **DFS** — краще для пошуку будь-якого шляху, коли оптимальність не важлива
2. **BFS** — необхідний, коли потрібен найкоротший шлях у невваженому графі
3. **Складність обох:** O(V + E), але пам'ять залежить від форми графа
4. **Для реальних задач** (навігація, соціальні мережі) **BFS переважно краще**
5. **Гібридні підходи** (A*, Dijkstra) поєднують переваги обох методів

---

## 📚 Рекомендації

- Для навігації на карті → **BFS** або **Dijkstra**
- Для обходу дерева рішень → **DFS**
- Для соціальної мережі (знайти спільних друзів) → **BFS**
- Для виявлення циклів → **DFS**

---

## 📝 Технічні деталі

- **Мова:** Python 3
- **Бібліотеки:** networkx, collections, matplotlib
- **Тип графа:** Неорієнтований граф
- **Кількість вершин:** 15
- **Кількість ребер:** 16